<?xml version="1.0" encoding="utf-8"?>
<winUiEx:WindowEx
    x:Class="perseverance.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winUiEx="using:WinUIEx"
    mc:Ignorable="d"
    Title="perseverance"
    Height="1050"
    Width="1275">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <TitleBar
            x:Name="TitleBar">
        </TitleBar>
        
        <NavigationView
            x:Name="NavigationView"
            Grid.Row="1"
            Style="{StaticResource NavigationViewStoreStyle}"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="False">
            
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="Home" Icon="Home" />

                <NavigationViewItem x:Name="Mods">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE74C;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            
            <NavigationView.FooterMenuItems>
                <NavigationViewItem x:Name="Downloads">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEBD3;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Name="Library" Icon="Library" />
            </NavigationView.FooterMenuItems>
            
            <Frame />
        </NavigationView>
    </Grid>
</winUiEx:WindowEx>
